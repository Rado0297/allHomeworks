.container
    .jumbotron
        h1 
            img(src="../../pictures/mathpro.png", width="100px", height="100px")
            | &nbsp Добре дошли в MathPro
    .row
        .col-md-6
            .panel
                .panel-heading.text-center.crimson
                    a.geoAndAlg(href="#/geometry") Геометрия
                .panel-body
                    #contentGeometryPanel
                        p   Тук може да решите вашите задачи по математика от дял "Геометрия"
                    img#geomStart(src="../../pictures/geometryStart.png")

        .col-md-6
            .panel
                .panel-heading.text-center.algebra-tab-color
                    a.geoAndAlg(href="#/algebra") Алгебра
                .panel-body
                    #contentAlgebraPanel
                        p ax + ay + az = a(x + y + z)
                        p x² - y² = (x + y)(x - y)
                        p x² + 2xy + y² = (x + y)²
                        p x² - 2xy + y² = (x - y)²
                        p x³ + y³ = (x + y)(x² - xy + y²)
                        p x³ - y³ = (x - y)(x² + xy + y²)
                    #fullScreenSize(font-size="21px")
                        p ax + ay + az = a(x + y + z)
                        p x² - y² = (x + y)(x - y)
                        p x² + 2xy + y² = (x + y)²
                        p x² - 2xy + y² = (x - y)²
                        p x³ + y³ = (x + y)(x² - xy + y²)
                        p x³ - y³ = (x - y)(x² + xy + y²)